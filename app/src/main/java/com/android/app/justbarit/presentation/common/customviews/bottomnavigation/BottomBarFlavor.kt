package com.android.app.justbarit.presentation.common.customviews.bottomnavigation

/**
 * [BottomBarFlavor] decides which type (icon, title) will
 * be shown as default (unselected).
 * The default flavor is [BottomBarFlavor.ICON].
 */
enum class BottomBarFlavor(val value: Int) {
  ICON(0)
}
